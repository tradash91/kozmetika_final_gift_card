/* export const getPosts = async () => {
  const res = await fetch(
    `${import.meta.env.VITE_API_URL}/v1/blog/getPosts`, 
    {
      method: "GET",

      
    }
  );

  if (!res.ok) throw new Error("You are not authenticated");
  const data = await res.json();
  return data;
};
 */
